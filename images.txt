# Docker 镜像同步列表
# 格式：每行一个镜像地址，支持 tag，支持私有仓库路径
# 使用 # 开头作为注释
# 所有架构会自动同步（--all），无需指定 --platform

# === 示例（取消注释即可启用）===

# 基础镜像
# nginx:latest
# alpine:latest

# 带命名空间的镜像
# xhofe/alist:latest
# fatedier/frps:v0.61.2

# 私有仓库镜像
# k8s.gcr.io/kube-state-metrics/kube-state-metrics:v2.0.0
# registry.k8s.io/ingress-nginx/controller:v1.12.1

# AI/ML 镜像
# vllm/vllm-openai:v0.8.5
# rayproject/ray:2.41.0

# 当前启用
jxxghp/moviepilot-v2:latest
